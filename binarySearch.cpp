#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, k;
    int tmp;
    vector<int> nums;
    cin >> n >> k;
    for(int i = 0; i < n; i++){
        cin >> tmp;
        nums.push_back(tmp);
    }
    
    for(int i = 0; i < k; i++){
        int l = 0; 
        int r = nums.size()-1;
        while(l >= r){

        }
    }
    cout << nums[9];


}